package ovh.adiantek.android.androjavame;

import android.graphics.Canvas;
import android.view.View;

class MIDletDisplay extends View {
    MIDletDisplay(MainActivity activity) {
        super(activity);
    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
    }
}
